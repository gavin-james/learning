# 表分片
spring:
  datasource:
    driverClassName: com.mysql.jdbc.Driver
    # 使用Druid，写成url。使用Hikari写成jdbc-url
    url: jdbc:mysql://localhost:3306/gavin_test?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8
    username: root
    password: root
    type: com.zaxxer.hikari.HikariDataSource